<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:theme="@android:style/Theme.Material.Light"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="fi.antonlehmus.activityreminder.SettingsActivity">

    <!--Resume on boot -->
    <Switch
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/resume_on_reboot"
        android:textSize="@dimen/label_text_size"
        android:switchMinWidth="56dp"
        android:id="@+id/resumeOnBoot"
        android:layout_marginBottom="@dimen/activity_vertical_margin" />

    <!--Silent hours label-->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/label_text_size"
        android:text="@string/silent_hours"
        />

    <!--Silent hours start-->
    <include layout="@layout/silent_hours_start"/>

    <!--Silent hours stop-->
    <include layout="@layout/silent_hours_stop"/>

    <!--Remind interval-->
    <include layout="@layout/remind_interval"/>

    <!--Step trigger count-->
    <include layout="@layout/step_trigger_count"/>

    <!-- to fill space -->
    <FrameLayout
        android:id="@+id/space_filler"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1"
        >
    </FrameLayout>

    <!--Buttons-->
    <include layout="@layout/start_stop_buttons"/>

    <!--Add placeholder-->
    <include layout="@layout/adds"/>


</LinearLayout>
